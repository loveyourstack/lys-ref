# services

Packages that provide high-level functionality from lower-level packages.

Examples:
* in this app: syssvc combines many database store select calls to provide a single output
* in loveyourstack/connectors: syncing external data into the database (uses external API calls and store packages)